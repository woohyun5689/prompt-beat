# Beat Anchor Maps

`Tools/Rhythm/Beat Anchor Editor` stores per-song `*.anchors.json` sidecars here.
The note-chart generator validates each sidecar against the source audio hash and
uses the anchors as a piecewise beat grid when rebuilding charts.
